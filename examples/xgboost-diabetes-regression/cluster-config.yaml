# XGBoost Diabetes Regression Spark Example - Compute Cluster Configuration
# Usage: darwin compute create --file examples/xgboost-diabetes-regression/cluster-config.yaml
#
# This configuration creates a compute cluster with Darwin SDK runtime
# which includes Ray + Spark support for distributed XGBoost training.

name: 'diabetes-xgboost-spark-example'
tags: ["test", "diabetes-regression", "xgboost", "spark"]
runtime: '1.0'                    # Darwin SDK runtime with Spark support
terminate_after_minutes: 10       # Auto-terminate after 10 minutes of inactivity
user: 'test@abc.com'

# Head node configuration
head_node:
  node:
    cores: 1
    memory: 1                     # 1GB memory
    node_capacity_type: 'ondemand'

# Worker node configuration
worker_group:
  - node:
      cores: 1
      memory: 1                   # 1GB memory per worker
      node_capacity_type: 'spot'
    min_pods: 1                   # Minimum 1 worker
    max_pods: 2                   # Scale up to 2 workers
